spring:
  datasource:
    url: jdbc:mysql://localhost:3306/
    name: webflux_test
  r2dbc:
    url: r2dbc:tc:mysql:///${datasource.name}?TC_IMAGE_TAG=latest
    username: root
    password: Zvezda002
  flyway:
    enabled: false

